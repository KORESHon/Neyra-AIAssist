# HTTP-маршруты собираются в ядре; точка входа процесса — core/server.py (run_neyra_server).
# Этот main.py — совместимость с PluginLoader / будущий invoke.
id: internal_api
name: Internal API
description: REST + WebSocket gateway (FastAPI /v1).
version: "1.0.0"
enabled: true
lifecycle: resident
cli_modes: []
main_script: main.py
