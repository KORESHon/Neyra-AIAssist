# Copy to `config.yaml`. Merged as config.plugins.local_voice.
# RU: Скопируйте в config.yaml — в корне будет plugins.local_voice.
#
# EN: Enable/disable in interfaces/local_voice/plugin.yaml (`enabled:`).

# EN: Wake word for local voice trigger.
wake_word: "neyra"

# EN: Input audio device name/id (optional, OS-dependent).
input_device: ""

# EN: Output audio device name/id for playback.
output_device: ""

# EN: Virtual cable output device (for routing to other apps).
vb_cable_output_device: ""
