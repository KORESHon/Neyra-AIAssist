# Локальный деплой

- Используйте `python main.py`.
- По умолчанию API на `127.0.0.1:8787`.
- Frontend статика берётся из `frontend/dist` (если собрана).
- Для разработки фронтенда: `cd frontend && npm run dev` (proxy на backend).
