"""
Neyra core package.

Layout (Phase 1R):
- ``core/neyra.py`` — main orchestrator (``NeyraAgent``); the only non-package module in ``core/``.
- Packages: ``agent`` (helpers), ``memory``, ``llm``, ``plugins``, ``runtime``, ``reflection``, ``tools``, ``voice``.
"""
